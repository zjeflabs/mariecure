# yaml-language-server: $schema=https://unpkg.com/@sveltia/cms/schema/sveltia-cms.json

backend:
  name: github
  repo: zjeflabs/mariecure
  branch: main
  # Aanmelden gaat via een wachtwoordpagina op dit domein (/api/auth, een Vercel
  # serverless function). Die geeft het CMS een repo-token terug. Zo heeft Marieke
  # geen eigen GitHub-account nodig — enkel het master-wachtwoord.
  base_url: https://www.mariecure.be/api

# Nieuwe foto's worden hier geupload; bestaande foto's blijven in /assets/img.
media_folder: assets/img/gallery
public_folder: /assets/img/gallery

locale: nl

collections:
  - name: site
    label: Website
    files:
      - name: gallery
        label: "Foto's (galerij)"
        file: assets/data/gallery.json
        description: "Voeg foto's toe of verwijder ze. De volgorde hier is de volgorde op de website."
        fields:
          - label: "Foto's"
            name: photos
            label_singular: Foto
            widget: list
            summary: "{{fields.alt}}"
            fields:
              - { label: Afbeelding, name: image, widget: image }
              - { label: "Omschrijving (alt-tekst)", name: alt, widget: string, required: false }
